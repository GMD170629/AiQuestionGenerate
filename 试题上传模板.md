 [单选题] [难度1] [分数1.0]下面能正确进行字符串赋值操作的是（    ）。

[A]char s[5] = {"ABCDE"};  

[B]char s[5] = {'A', 'B', 'C', 'D', 'E'};

[C]char *s; s = "ABCDE";   

[D]char *s;  scanf ("%s", s);

[答案]C

[章节]2.1信息与编码

[知识点]信息与编码

[解析] C语言中，字符串必须是以'\0'结尾。在答案A中，s定义为5个元素的字符型数组，即最多能存放5个字符，字符串结尾符'\0'无法存放，故A是错误的字符串赋值；答案B对数组赋值没错，但同A一样，没有字符串结尾符'\0'，所以也是错误的；答案D是“野指针”赋值，是错误的；而对于答案C，先定义字符指针s，而后将字符串"ABCDE"的首地址赋值给指针变量s，是正确的赋值。所以正确答案是C。

 

[多选题][难度:2][分数:1.0]下面能正确进行字符串赋值操作的是（    ）。

[A]char s[5] = {"ABCDE"};  

[B]char s[5] = {'A', 'B', 'C', 'D', 'E'};

[C]char *s; s = "ABCDE";   

[D]char *s;  scanf ("%s", s);

[答案]CD

[章节]1.4.2常见的微型计算机硬件

[知识点]信息与编码、常见的微型计算机硬件

[解析]C语言中，字符串必须是以'\0'结尾。在答案A中，s定义为5个元素的字符型数组，即最多能存放5个字符，字符串结尾符'\0'无法存放，故A是错误的字符串赋值；答案B对数组赋值没错，但同A一样，没有字符串结尾符'\0'，所以也是错误的；答案D是“野指针”赋值，是错误的；而对于答案C，先定义字符指针s，而后将字符串"ABCDE"的首地址赋值给指针变量s，是正确的赋值。所以正确答案是C。

 

 

[填空题][难度:2][分数:1.0]多个空的填空题演示，mystrlen函数的功能是计算str所指字符串的长度，并作为函数值返回。请填空。

int mystrlen(char *str)

{

  int i;

  for (i = 0;  ______ ; i++);

  return (_____ );

}

 

 

[答案]【1】*(str + i) != '\0' 、*(str + i) != '\0'  【2】i

[章节]3.1操作系统概述

[解析]p最初指向数组arr的第一个元素30，执行p++;后，p指向arr的第二个元素25，*(p+3)其实就是取数组中第5个元素的值，即10。

 

 

[填空题][难度:2][分数:1.0]以下程序的输出结果是__________。

void main( )

{

  int arr[ ]={30, 25, 20, 15, 10, 5}, *p=arr;

  p++;

  printf("%d\n", *(p+3));

}

[答案]错误

[章节]3.1操作系统概述

[知识点]操作系统概述、操作系统

[解析]p最初指向数组arr的第一个元素30，执行p++;后，p指向arr的第二个元素25，*(p+3)其实就是取数组中第5个元素的值，即10。

 

[编程题][难度:2][分数:1.0]字符反序输出。

[题目描述]编写一个函数读取用户输入的字符，然后将其反序输出

[输入说明]一个长度不大于10的字符串

[输出说明]将输入的字符反序输出

[输入样例]432

[输出样例]234

[答案]

\#include <stdio.h>

\#define SIZE 10

void swap (int *x, int *y);

void main (  )

{

 int a[SIZE], b[SIZE], i, n;

 printf ("Input array length n<=%d: ", SIZE);

 scanf ("%d", &n);

 printf ("Input array a: ");

 for (i = 0; i < n; i++)

   scanf ("%d", &a[i]);

 printf ("Input array b: "); 

 for (i = 0; i < n; i++)

   scanf ("%d", &b[i]);

 for (i = 0; i < n; i++)  //调用交换函数，实现两数组元素互换

   swap (&a[i], &b[i]);

 printf ("After swap:\n");

 printf ("Array a: ");

 for (i = 0; i < n; i++)

   printf ("%d ", a[i]);

 printf ("\n");

 printf ("Array b: ");

 for (i = 0; i < n; i++)

   printf ("%d ", b[i]);

 printf ("\n");

}

//交换整型指针x和y所指向的两个整数

void swap (int *x, int *y)

{

  int temp;

  temp = *x;

  *x = *y;

  *y = temp;

}

[章节]3.1操作系统概述

[题 解]用指针变量作为函数参数实现两数交换。定义两个指针变量作为形参，在调用时分别指向两个数组的相应位置。

 

 

[编程题][难度:2][分数:1.0] 字符转数字

[题目描述]将一个长度不超过4的数字字符串转换成一个整数。如字符串是“3248”，则转换的整数是3248。

[输入说明] 长度不超过4位的数字字符串，如“3248”

[输出说明] 转换后的数字

[输入样例1] “3248”

[输出样例1] 3248

[输入样例2]”7966”

[输出样例2]7966

[答案]

\#include <stdio.h>

void main( )

{

 static char x[4];

 char *p = x;

 int s = 0;

 gets(p);

 while(*p != '\0')

 { 

  s = s*10 + *p - '0';

  p++;

 }

 printf("%d", s);

}

[章节]3.1操作系统概述

[知识点]字符、循环

[解析] 对数字字符串从左到右逐一取出并转换为数字（数字字符减除0字符），然后重新组成一个整数。

 

[简答题][难度:1][分数:2.0]简要说明计算机学科的三个过程。

[答案]三个过程包括抽象过程、理论总结过程和设计过程。抽象是指在思维中对同类事物去除其现象的、次要的方面，抽取其共同的、主要的方面，从而做到从个别中把握一般，从现象中把握本质的认知过程和思维方法。科学认识由感性阶段上升为理性阶段，就形成了科学理论。通过对现实事物的分析、抽象，并对其本质性的一般规律进行总结、升华，就形成理论。设计过程源于工程学，用来开发求解给定问题的系统和设备。主要要素为需求说明、规格说明、设计和实现方法、测试和分析。

[章节]3.1操作系统概述

[知识点]操作系统、三个过程

[解析] 用指针变量作为函数参数实现两数交换。定义两个指针变量作为形参，在调用时分别指向两个数组的相应位置。

 